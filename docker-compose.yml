version: '3'
services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "3000:80"
  node-app:
    build: .
    environment:
      - PORT=3000
    depends_on:
      - mongo

  mongo:
    image: mongo
    environment:
      - MONGO_INITDB_ROOT_USERNAME=youssef
      - MONGO_INITDB_ROOT_PASSWORD=azerty
    volumes:
      - mongo-db:/data/db
  
  redis:
    image: redis

volumes:
  mongo-db:
